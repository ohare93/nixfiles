# Nix build output symlinks
result
result-*

# Private configuration (encrypted version is secrets/private.nix.age)
private.nix

# Juggler task tracking (local state)
.juggle

# Sandbox/editor artifacts
.bash_profile
.bashrc
.gitconfig
.gitmodules
.profile
.ripgreprc
.zprofile
.zshrc
.idea
.vscode
.mcp.json

